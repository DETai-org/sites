# Taxonomy Policy (Source of Truth + i18n)

## 1) Цель
Зафиксировать единый "Source of Truth" для таксономии (rubrics / categories / keywords),
с переводами на 5 языков, так чтобы оба сайта (detai-site и personal-site) могли
использовать один общий слой статических данных без дублирования структуры.

---

## 2) Где лежит источник истины
Источник истины для таксономии в репозитории `sites` хранится как статические данные в workspace-пакете:

`/packages/static-data/taxonomy.base.json`

Этот файл является **канонической версией** таксономии, которую используют оба сайта.

---

## 3) Языки
Поддерживаемые языки фиксированы:

`ru`, `en`, `de`, `fi`, `cn`

Правило качества:
- **каждое** переводимое поле в каждом элементе таксономии должно быть заполнено на всех 5 языках;

---

## 4) Идентификаторы и slug 

см. в route_slugs_policy.md

---

## 5) Формат taxonomy.base.json 
Файл `taxonomy.base.json` 
объединённые rubrics/categories/keywords с переводами


authors.base.json - хранит имена и фамилии авторов на всех языках. Соответвующий язык имени и фамилии автора подстовляется 
в место где у статьи в блоге указан автор

pageMeta.base.json (базовые title/description для разделов);
