# Post Creation Prompt (Блоговые посты)

## 🎯 Назначение
Этот документ — **единый источник правил** для обработки новых блог‑постов и их публикации на сайтах `detai-site` и/или `personal-site`.
Он описывает **порядок действий** и **ссылается на политики**, не дублируя сложные правила.

---

## ✅ Контекст и источники истины

Перед работой сверяйся с каноном:

**Политики (Source of Truth):**
- `packages/static-data/policies/taxonomy_policy.md`
- `packages/static-data/policies/route_slugs_policy.md`
- `packages/static-data/policies/post_slugs_policy.md`
- `packages/static-data/policies/post_images_policy.md`

**Процессы добавления постов:**
- `detai-site/lib/blog/README.md`
- `personal-site/lib/blog/README.md`

**Таксономия (единый источник):**
- `packages/static-data/post_documents/` (`rubrics.json`, `categories.json`, `keywords.json`)

---

## 📌 Входные данные (что передаётся агенту)

**Обязательное (минимум):**
- один или два Markdown‑файла постов **на русском** с frontmatter;
- `administrative.id`, `administrative.date_ymd`, `administrative.status`, `administrative.channels`;
- `descriptive.title`, `descriptive.preview`, `descriptive.seoLead`;
- `descriptive.taxonomy.rubric_ids`, `category_ids`, `keyword_ids`, `keywords_raw`.

**Дополнительно пукт 1.2
Эти данные могут быть переданы от пользователя а могут быть и не переданы
- `coverImage`:
  - имя файла или абсолютный URL,
  - ширина/высота,
  - `alt` (одна строка, без локализации).
  - если `coverImage` не задан — пост публикуется **без обложки**  @ (TODO: обсудить стандартную заглушку);
  - если `alt` пустой или не указан — агент **сам генерирует** SEO‑дружественный `alt`, согласованный с заголовком.
- `author` (если указано явно; если нет берётся из `administrative.authors[0]`).

---

## 📂 Где лежат входящие посты

Посты могут попадать во временные staging‑директории:
- `docs/publications/blogs/detai_site_blog/`
- `docs/publications/blogs/personal_site_blog/`

Это **временные контейнеры** — файлы оттуда берутся для сборки и переноса в рабочие папки сайтов.
Ориентируйся на **последние по времени добавления** файлы или на явное указание пользователя.

Правила определения «нового» поста:
- в каждой папке одновременно хранится **только один актуальный пост** для этого сайта;
- новый пост — это **последний по времени добавления** файл в папке;
- если в обеих папках есть по одному посту — это означает публикацию **на обоих сайтах**;
- для проверки можно посмотреть, опубликован ли пост на соответствующем сайте: если отсутствует, значит это **актуальный** материал.

---

## 🧭 Ключевое правило маршрутизации

Поле `administrative.channels` — **единственный источник истины**:
- `detai_site_blog` → публикуем в `detai-site`
- `personal_site_blog` → публикуем в `personal-site`
- если указаны оба канала — публикуем **на обоих сайтах**

---

## 🧩 Требования к frontmatter (на всех языках)

Поддерживаемые языки: **ru, en, de, fi, cn**.

Для каждого языка должен существовать файл `<lang>.md` с frontmatter:

```yaml
---
type: post
administrative:
  id: "<postId>"
  authors:
    - "<Author Name>"
  date_ymd: "YYYY-MM-DD"
  status: publish
  channels:
    - detai_site_blog
    - personal_site_blog
descriptive:
  routeSlug: "<localized-slug>"
  title: "<localized-title>"
  preview: "<localized-preview>"
  seoLead: "<localized-seoLead>"
  taxonomy:
    rubric_ids: ["rubric:..."]
    category_ids: ["category:..."]
    keyword_ids: ["keyword:..."]
    keywords_raw: ["локализованные ключевые слова"]
structural:
  external_links: []
  document_links: []
---
```

Важно:
- `routeSlug` — локализованный для каждого языка (см. `post_slugs_policy.md`).
- `keywords_raw` локализуется (переводы на каждом языке).
- `rubric_ids`, `category_ids`, `keyword_ids` — стабильные id, не переводятся.

---

## 🛠️ Процесс обработки (для каждого поста)

1. Определи сайт(ы) по `administrative.channels`.
2. Проверь полноту frontmatter на RU (минимальные поля).
3. Создай/обнови все языковые файлы:
   - сгенерируй `routeSlug` на каждом языке по правилам,
   - переведи `title`, `preview`, `seoLead`, `keywords_raw`.
4. Размести пост в каждом сайте:
   - `detai-site/lib/blog/posts/<postId>/<lang>.md`
   - `personal-site/lib/blog/posts/<postId>/<lang>.md`
5. Обнови `blog.base.ts` в каждом сайте, где есть пост:
   - `postId` = `administrative.id`
   - `publishedAt` = `administrative.date_ymd` (в ISO с TZ по умолчанию проекта)
   - `author` = первая запись из `administrative.authors`
   - `status`, `rubric`, `category`, `keywords`, `keywordsRaw`
   - `coverImage` (см. политику изображений)
   - `contentFiles` для всех языков
6. Обложка:
   - если пост общий для обоих сайтов — допускается единая обложка в `personal-site/public/images/posts/` + абсолютная ссылка для `detai-site` (см. `post_images_policy.md`).

---

## ✅ Проверки перед завершением

- Есть `<lang>.md` для всех 5 языков.
- `routeSlug` заполнен во всех языках.
- `contentFiles` корректно указывает на файлы.
- Таксономия (`rubric_ids`, `category_ids`, `keyword_ids`) существует в `packages/static-data/post_documents/`.
- `coverImage` заполнен или соответствует политике (пукт 1.2 этого файла)
