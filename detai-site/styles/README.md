# 🟡 globals.css - за что отвечает?

globals.css — это минималистичный файл.

Он включает:

@tailwind base;
@tailwind components;
@tailwind utilities;


И если нужно — добавляет:

глобальные reset'ы

html, body стили

кастомные переменные root вынесены в `styles/theme/`

глобальные keyframes

импорты шрифтов (если шрифты кастомные)

И всё 🟦.

Палитра и токены темы живут отдельно, чтобы не перегружать globals.

## 🌗 theme/

Семантические токены темы вынесены в отдельную папку:

- `styles/theme/tokens.css` — значения для светлой темы.
- `styles/theme/overrides.css` — переопределения для `.dark`.

`globals.css` подключает их одной строкой и не содержит цветовых палитр напрямую.

## 🎇 ui-effects.css

Файл для общих CSS-эффектов (например, shimmer для кнопок). Импортируется из `globals.css`.

## 📄 backgrounds.css

Набор фоновых классов с бумажными текстурами. Подходит для любых блоков (`section`, `div`, карточки) без дублирования стилей. Подключается из `globals.css`.

- `.paper--ambient` — десктопный вариант с повторяющейся текстурой и мягким оверлеем, скрывающим стыки.
- `.paper--object` — базовый лист бумаги с отдельным размытием нижнего края.
- `.paper--object-mobile` — мобильный вариант листа бумаги с повторяющейся текстурой для карточек на маленьких экранах.
- `.paper-shape--scroll` — утилита для формы свитка через CSS mask.
- `.paper-edges--scroll` — утилита для маски краёв свитка поверх бумажной текстуры.

## 🪄 project-card.states.css

Файл для визуальных состояний карточек проектов (ready, in-progress и будущие статусы). Подключается через `globals.css` и отвечает только за состояния, не за базовую сетку или эффекты.
