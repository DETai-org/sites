# 🟡 globals.css - за что отвечает?

globals.css — это минималистичный файл.

Он включает:

@tailwind base;
@tailwind components;
@tailwind utilities;


И если нужно — добавляет:

глобальные reset'ы

html, body стили

кастомные переменные root (если они нужны вообще)

глобальные keyframes

импорты шрифтов (если шрифты кастомные)

И всё 🟦.

Никаких палитр цветов в globals.css — НЕ НУЖНО.

## 🎇 ui-effects.css

Файл для общих CSS-эффектов (например, shimmer для кнопок). Импортируется из `globals.css`.

## 📜 backgrounds.css

Отдельные утилиты фонов, которые сложно собрать из Tailwind-классов. Сюда вынесены сложные текстуры (например, бумажный фон
для мобильной карточки DETai), чтобы их можно было подключать как независимые классы (`bg-mobile-paper`).
