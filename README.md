
# DETai Site — Next.js + Vercel

Официальный репозиторий фронтенд-сайта экосистемы **DETai**.  
Проект основан на **Next.js (App Router, TypeScript)** и разворачивается через **Vercel**.  
Фронтенд управляется через GitHub, а сборку проекта выполняет Vercel автоматически при каждом пуше.

Этот сайт — визитка экосистемы DET: структура, продукты, боты, концепции и вся публичная часть проекта.

---

## 🚀 Основные технологии

- **Next.js 14** (App Router)
- **React**
- **TypeScript**
- **Tailwind CSS**
- **Vercel**
- **i18n JSON-файлы (многоязычие)**
- **Компонентная архитектура**

---

## 📦 Структура проекта

```bash
detai-site/
│
├── app/                     # Страницы и маршруты (App Router)
│   ├── layout.tsx           # Главный layout (обертка для всех страниц)
│   ├── page.tsx             # Главная страница
│   ├── about/page.tsx       # О проекте
│   ├── products/page.tsx    # Список продуктов/ботов экосистемы
│   ├── privacy/page.tsx     # Политика конфиденциальности
│   └── ...                  # Дополнительные страницы
│
├── components/              # UI-компоненты
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── LanguageSwitcher.tsx
│   ├── ProductCard.tsx
│   └── Layout.tsx
│
├── styles/
│   └── globals.css          # Глобальные стили (Tailwind)
│
├── public/                  # Публичная директория (доступна напрямую)
│   ├── favicon.ico          # Иконка вкладки
│   ├── logo.png             # Основной логотип DETAI
│   │
│   ├── robots.txt           # Инструкции для поисковых систем
│   ├── sitemap.xml          # Карта сайта
│   │
│   ├── manifest.json        # Опционально: для PWA
│   ├── browserconfig.xml    # Опционально: плитки Windows
│   │
│   ├── images/              # Основная медиатека
│   │   ├── products/        # Картинки для ботов/продуктов DET
│   │   ├── ui/              # UI-элементы
│   │   ├── backgrounds/     # Фоны
│   │   └── ...
│   │
│   └── assets/              # Всё, что не изображения
│       ├── privacy.pdf
│       ├── terms.pdf
│       ├── demo.mp4
│       └── data.json
│
├── i18n/                    # Файлы локализации
│   ├── ru.json
│   ├── en.json
│   ├── de.json
│   ├── fi.json
│   └── cn.json
│
├── next.config.js           # Конфигурация Next.js
├── package.json             # Зависимости и npm-скрипты
├── tailwind.config.js       # Конфиг Tailwind
├── postcss.config.js        # PostCSS настройки
└── README.md                # Этот файл
````

---

## 🌍 Многоязычие (i18n)

Локализация реализована через JSON-файлы в директории `/i18n`.

Каждый файл — набор ключей:

```json
{
  "title": "Добро пожаловать в DETAI",
  "description": "Экосистема продуктов и интеллектуальных инструментов."
}
```

Компоненты используют локализацию через простые хелперы (добавит Codex).

---

## 🎨 Компонентная система

Все элементы UI вынесены в `/components`.
Каждый компонент — атом интерфейса:

* `Header` — навигация + логотип + языки
* `Footer` — нижняя часть сайта
* `ProductCard` — карточка продукта / бота
* `LanguageSwitcher` — переключение языка
* `Layout` — базовая обёртка страниц

Codex будет постепенно расширять эти компоненты.

---

## 🚀 Деплой (Vercel)

Проект автоматически разворачивается на:

```
https://detai-site.vercel.app
```

После каждого `git push`
Vercel запускает:

1. установку зависимостей
2. сборку Next.js
3. размещение на CDN
4. обновление прод-версии сайта

Настройки деплоя хранятся в Vercel Dashboard.

---

## 🧱 Архитектура проекта

Фронтенд и backend разделены:

* **Фронтенд (этот проект)** → Vercel
* **Backend / API / Боты** → DigitalOcean (отдельный репозиторий)

Сайт обращается к backend через REST API (когда он будет создан).

---

## 🧪 Локальный запуск

После клонирования:

```bash
npm install
npm run dev
```

Сайт будет доступен по адресу:

```
http://localhost:3000
```

---

```

