
# DETai Site — Next.js + Vercel

Официальный репозиторий фронтенд-сайта экосистемы **DETai**.  
Проект основан на **Next.js (App Router, TypeScript)** и разворачивается через **Vercel**.  
Фронтенд управляется через GitHub, а сборку проекта выполняет Vercel автоматически при каждом пуше.

Этот сайт — визитка экосистемы DETai: структура, продукты, боты, концепции и вся публичная часть проекта.

---

## 🚀 Основные технологии

- **Next.js 14** (App Router)
- **React**
- **TypeScript**
- **Tailwind CSS**
- **Vercel**
- **i18n JSON-файлы (многоязычие)**
- **Компонентная архитектура**

---

## 🤟 Главная страница наш образец 

Hero — h1: «DET — Dialectical Existential Therapy.» — шрифт Lora (класс font-serif).

От DET к DETai — h2: «От DET к DETai» — шрифт Lora.

Проекты DETai — h2: «Проекты DETai» — шрифт Lora.

Выберите направление — h2: «Выберите направление» — шрифт Lora.

Для кого DET и DETai — h2: «Для кого DET и DETai» — шрифт Lora.

Карточки внутри блока:

h3: «Психологам и терапевтам» — Lora.

h3: «Людям, идущим путём внутреннего развития» — Lora.

h3: «Исследователям и авторам» — Lora.

h3: «Создателям технологий и продуктов» — Lora.

Основания DET — h2: «Основания DET» — шрифт Lora.

Наша миссия — h2: «Наша миссия» — шрифт Lora.

## 📦 Структура проекта

```bash
detai-site/
│
├── scripts/                      # Служебные скрипты и утилиты
│   └── inspect-lottie.js         # Анализатор Lottie-анимации logo_intro.json
│
├── app/                           # Страницы и маршруты (App Router)
│   ├── layout.tsx                 # Главный layout
│   ├── page.tsx                   # Главная
│   ├── about/page.tsx             # О проекте
│   ├── products/page.tsx          # Продукты/боты экосистемы
│   ├── privacy/page.tsx           # Политика конфиденциальности
│   ├── (marketing)/               # Пример route group (не влияет на URL)
│   └── products/_components/      # Локальные компоненты страницы
│       ├── ProductHeader.tsx
│       └── ProductTile.tsx
│
├── components/                    # Глобальные UI-компоненты проекта
│   ├── ui/                        # Примитивы (atoms)
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Card.tsx
│   │   └── Icon.tsx
│   │
│   ├── layout/                    # Каркас сайта
│   │   ├── Header.tsx
│   │   ├── Footer.tsx
│   │   └── LayoutWrapper.tsx
│   │
│   └── sections/                  # Крупные блоки страниц (molecules/organisms)
│       ├── HeroSection.tsx
│       ├── FeaturesSection.tsx
│       └── ProductsSection.tsx
│
├── styles/
│   └── globals.css          # минималистичный файл т.к. см. tailwind.config.js
│
├── public/                  # Публичная директория (доступна напрямую)
│   ├── favicon.ico          # Иконка вкладки
│   ├── logo.png             # Основной логотип DETAI
│   │
│   ├── robots.txt           # Инструкции для поисковых систем
│   ├── sitemap.xml          # Карта сайта
│   │
│   ├── manifest.json        # Опционально: для PWA
│   ├── browserconfig.xml    # Опционально: плитки Windows
│   │
│   ├── images/              # Основная медиатека
│   │   ├── products/        # Картинки для ботов/продуктов DET
│   │   ├── ui/              # UI-элементы
│   │   ├── backgrounds/     # Фоны
│   │   └── ...
│   │
│   └── assets/              # Всё, что не изображения
│       ├── privacy.pdf
│       ├── terms.pdf
│       ├── demo.mp4
│       └── data.json
│
├── i18n/                    # Файлы локализации
│   ├── ru.json
│   ├── en.json
│   ├── de.json
│   ├── fi.json
│   └── cn.json
│
├── next.config.js           # Конфигурация Next.js
├── package.json             # Зависимости и npm-скрипты
├── tailwind.config.js       # Конфиг Tailwind
├── postcss.config.js        # PostCSS настройки
└── README.md                # Этот файл
````

## 📐 Layout-правила страниц

* Главная и большинство страниц используют базовый контейнер `max-w-6xl` с десктопными отступами `px-10`.
* Каталог проектов `/detai/projects` разворачивается как витрина: расширенный контейнер `md:max-w-[96rem]` (`max-w-screen-2xl`) с симметричными `md:px-16` создаёт комфортную ширину без full-bleed. Все секции страницы (заголовки, описания, эшелоны и карусели) выровнены по одной левой вертикали в этом контейнере, что отличает страницу от главной.

---

## Основные файлы

**package.json**\
Именно здесь Next.js, Tailwind, Vercel и весь проект понимают, **кто мы, какие зависимости нам нужны и какие команды запускать**.

🔥 Этот файл отвечает за:
> список всех зависимостей проекта (dependencies, devDependencies)
> npm-скрипты (`dev`, `build`, `lint`, `start` — всё тут)
> базовую мета-информацию о проекте (название, версия)
> настройки инструментов (eslint, prettier, lint-staged и др.)
> **Codex** ориентируется именно на `scripts`, поэтому здесь должны быть корректные команды разработки и сборки.


**next.config.js** 

Глобальный конфигурационный файл Next.js.\
Управляет:
> включёнными экспериментальными фичами\
> перенаправлениями (redirects) и переписыванием URL (rewrites)\
> оптимизациями изображений\
> настройками middleware\
> переменными окружения (частично)\
> поведением сборки

Если нужны дополнительные возможности — добавляются сюда.
В большинстве случаев достаточно минимальной конфигурации.


**tailwind.config.js**\
Tailwind — это utility-first CSS, и он ожидает, что вы будете прописывать дизайн-токены в конфиге

🔥 Это позволяет:
> менять цвета в одном месте\
> давать семантические имена (primary, accent, warning)\
> использовать тёмную тему 🌘🤩\
> давать единый стиль сайту\
> уменьшить CSS


**postcss.config.js**\
PostCSS — это движок, который пропускает весь CSS через цепочку плагинов.
Этот файл — как «маршрутная карта», которая говорит сборке, какие плагины использовать.

🔥 Обычно тут всего два ключевых плагина:
> **TailwindCSS** — подхватывает Tailwind-классы
> **Autoprefixer** — добавляет префиксы для разных браузеров, чтобы всё выглядело одинаково
> управляет CSS-пайплайном
> автоматически применяется при сборке Next.js
> почти никогда не требует ручных правок
____

## Что означает каждая папка

### **app/** — страницы, маршруты и серверные компоненты.  

**app/(marketing)/** - это **Route groups**  
Папки вида `(folder)` позволяют группировать страницы, не влияя на URL.

Например:
> app/(marketing)/page.tsx → URL остаётся просто `/`
> Route groups используются для организации структуры, но не появляются в адресной строке.

**app/products/_components/**
Внутри каждой страницы (например, app/products/) может находиться папка `_components/`.  
В неё помещаются локальные компоненты, которые используются только на этой странице.  
Next.js игнорирует папки, начинающиеся с подчёркивания, поэтому они не создают маршрутов  
и служат только для организации кода.  


**components/** — глобальные UI-компоненты проекта: примитивы, каркас, крупные секции и визуальный слой. Подробнее см. в
[📘 components/README.md](components/README.md).

**styles/** — глобальные стили проекта.
Содержит файл `globals.css`, который подключается один раз в `app/layout.tsx`. Отдельных CSS-файлов обычно не требуется.\
См.(📘 [README](styles/README.md))

**public/** — публичная директория с файлами, доступными напрямую по URL.\  
Next.js отдаёт любые файлы из `public/` без обработки.\
См.(📘 [README](public/README.md))


###  i18n/ - 🌍 Многоязычие (i18n)

Локализация реализована через JSON-файлы в директории `/i18n`.\
Каждый файл — набор ключей:\
Компоненты используют локализацию через простые хелперы (добавит Codex).\

---

## 🛠 Служебные скрипты

### `scripts/inspect-lottie.js`

CLI-утилита для быстрой диагностики Lottie-файла `public/assets/animations/logo_intro.json`.

- **Запуск по умолчанию**: `node scripts/inspect-lottie.js`
- **Произвольный файл**: `node scripts/inspect-lottie.js path/to/animation.json`

Скрипт выводит JSON-отчёт с базовыми параметрами (v, fr, ip, op, w, h), списком слоёв и признаками масок/трек-маттов/trim paths и т.п., а также ищет целевые объекты по имени (PULSE_CTRL, Pulse, psi_left/right, circle_*, arrow_*, p/i/k) с их трансформациями (static или keyframes для p/s/r/o).

---

## Фронтенд и backend разделены:

**Фронтенд (этот проект)** → Vercel. **Backend / API / Боты** → DigitalOcean (отдельный репозиторий)\
Сайт обращается к backend через REST API (когда он будет создан). API проекта находится вне этого репозитория.  
Фронтенд обращается к backend только через HTTP-запросы. Не создаем backend внутри detai-site.
